org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# Kotlin 2.4.0's incremental compiler cannot close its memory-mapped .tab caches
# on this machine, so every fresh plugin-module compile dies with
# "Could not close incremental caches in ...\caches-jvm\jvm\kotlin".
# No module in this project ships Kotlin beyond MainActivity, so the incremental
# path buys nothing here.
kotlin.incremental=false
